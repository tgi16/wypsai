# Gemini API Key for server-side proxy
GEMINI_API_KEY=

# Shared secret to protect /api/gemini endpoint (set same value in both)
# Server-side (Vercel env var):
WYPS_API_SECRET=
# Client-side (baked into build, must start with VITE_):
VITE_WYPS_API_SECRET=
